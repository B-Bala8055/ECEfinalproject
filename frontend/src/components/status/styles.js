import { makeStyles } from "@mui/styles"

const useStyles = makeStyles({
    container:{
        marginTop:10,
    },
    paperStyles:{
        margin:20,
        padding:20
    },
    message:{
        textAlign:'center',
        paddingTop:20,
        paddingBottom:20,
    }
})

export default useStyles